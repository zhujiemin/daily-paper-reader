---
title: What Breaks Local Watermarks? A Robustness Benchmark for Local Invisible Image Watermarking
title_zh: 什么会破坏局部水印？面向局部不可见图像水印的鲁棒性基准
authors: "Kai Yao, Bence Szilágyi, Sebestyén Kamp, Máté Poór, Máté Szilveszter, Matyas K. Zsoldos, Marc Juarez"
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.16832v1"
tags: ["query:diffusion-wm"]
score: 8.0
evidence: 局部不可见图像水印在图像变换下的鲁棒性基准
tldr: 局部不可见水印能将信号嵌入指定区域并从对象恢复载荷，但现有评估多聚焦单一方法，变换、数据集与指标不统一，难以横向比较。本文提出首个系统鲁棒性基准，覆盖55种图像变换，分为信号失真、坐标对齐变化、间接局部编辑和直接水印编辑四类，评估MaskWM、WAM、OmniGuard、TrustMark与PixelSeal。结果显示所有方法均对部分变换脆弱，MaskWM载荷恢复与定位最强但干净图像质量最低，同步可提升其几何变换表现却牺牲质量；几何错位和inpainting、outpainting等生成式局部编辑尤其致命。该基准揭示局部水印鲁棒性瓶颈与评测缺口。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.16832v1/fig-001.webp\", \"caption\": \"\", \"page\": 5, \"index\": 1, \"width\": 4578, \"height\": 902}, {\"url\": \"assets/figures/arxiv/2609.16832v1/fig-002.webp\", \"caption\": \"\", \"page\": 13, \"index\": 2, \"width\": 1980, \"height\": 1557}]"
motivation: 现有局部水印鲁棒性评估多聚焦单一方法，变换、数据集与指标不一致，导致方法间难以直接比较。
method: 构建首个系统鲁棒性基准，覆盖55种图像变换，分信号失真、坐标对齐、间接局部编辑与直接水印编辑四类，评估五种方法。
result: 所有方法均有脆弱变换；MaskWM载荷恢复与定位最强但干净图像质量最低，几何错位和生成式局部编辑可完全破坏恢复。
conclusion: 局部水印鲁棒性强依赖变换性质，载荷恢复与定位相关但不等价，基准揭示关键瓶颈与评测缺口。
---

## 摘要
局部图像水印将不可见信号嵌入到选定的图像区域，而不是将其分散到整幅图像中，从而能够从特定对象或区域恢复有效载荷，且不会明显改变图像。现有研究评估图像变换下有效载荷恢复和定位的鲁棒性，但往往聚焦于各自提出的方法，导致评估范围狭窄，且在变换、数据集和指标的选择上不一致。这些研究间的不一致限制了方法之间的直接比较，并使局部水印鲁棒性的整体图景变得模糊。为填补这一空白，我们提出了首个针对局部水印的系统性鲁棒性基准，涵盖55种图像变换，包括（i）信号失真，（ii）图像坐标对齐变化，（iii）间接局部编辑，以及（iv）直接水印编辑。该基准评估了 MaskWM、WAM、OmniGuard、TrustMark 和 PixelSeal，这些方法要么提供原生定位，要么只需最小适配即可支持定位。我们的结果表明，所有被评估方法都对某些变换脆弱，其中 MaskWM 在提供最强有效载荷恢复和定位方面表现突出，尽管它在干净设置下图像质量最低。同步进一步提升了 MaskWM 在若干几何变换下的有效载荷恢复，但代价是图像质量额外下降。一个关键发现是，局部水印鲁棒性强烈依赖于变换的性质：最强方法通常能容忍信号失真，而几何失配和生成式局部编辑（如修复和外绘）可能完全破坏有效载荷恢复。我们观察到，有效载荷恢复与定位相关但不可互换，并且二者都强烈依赖于变换对水印区域的影响。

## Abstract
Local image watermarking embeds an invisible signal into selected image regions rather than spreading it across the entire image, enabling payload recovery from specific objects or regions without perceptibly altering the image. Existing studies evaluate the robustness of payload recovery and localization under image transformations, but they often focus on their own proposed method, resulting in narrow evaluations with inconsistent choices of transformations, datasets, and metrics. These inconsistencies across studies limit direct comparisons across methods and muddle the overall picture of local watermark robustness. To address this gap, we present the first systematic robustness benchmark for local watermarks across 55 image transformations, including (i) signal distortions, (ii) changes in image coordinate alignment, (iii) indirect local edits, and (iv) direct watermark edits. The benchmark evaluates MaskWM, WAM, OmniGuard, TrustMark, and PixelSeal, all methods that either provide native localization or require minimal adaptation to support it. Our results show that all evaluated methods are vulnerable to some transformation, with MaskWM standing out as offering the strongest payload recovery and localization, although it has the lowest image quality in the clean setting. Synchronization further improves MaskWM's payload recovery under several geometric transformations, albeit at an additional cost to image quality. A key finding is that local watermark robustness depends strongly on the nature of the transformation: signal distortions are often tolerated by the strongest methods, while geometric misalignment and generative local edits, such as inpainting and outpainting, can completely impair payload recovery. We observe that payload recovery and localization are related but not interchangeable, and both strongly depend on the transformation's impact on the watermark region.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究背景**：生成式图像编辑使对象插入、擦除、重绘和跨来源拼合变得容易，传统“全图水印”难以对单个对象或局部区域提供来源追踪。局部图像水印因此受到关注：它把不可见信号嵌入指定区域，并尝试在解码时恢复载荷、定位水印区域。
- **核心问题**：现有局部水印鲁棒性评估通常由方法作者围绕自己提出的方案展开，变换集合、数据集、指标和定位目标不一致，导致不同方法之间难以公平比较，也难以判断局部水印在真实编辑流程下是否可靠。
- **论文含义**：本文提出首个系统性的局部不可见图像水印鲁棒性基准，覆盖 55 种非自适应图像变换，统一评估 5 种方法，并同时考察载荷恢复、定位精度和不可感知性。其结论具有安全含义：若非自适应黑盒变换即可移除水印，则实际攻击者可能更容易实现水印规避。

## 2. 方法论：核心思想与关键技术细节

- **统一评估流程**：每个评估样本分为三阶段：
  - **嵌入**：输入图像 + 水印掩码 + 随机二进制载荷；水印方法在掩码区域内嵌入载荷。若启用 SyncSeal，则在载荷嵌入后额外嵌入同步信号。
  - **变换**：对水印图像施加 55 种变换之一，或保留干净参考条件。
  - **盲解码**：解码器只接收可能被变换后的图像，不访问原始无水印图像。若启用 SyncSeal，先估计并逆转几何变换，再恢复载荷；支持定位的方法输出水印区域。
- **变换分类法**：将 55 种变换分为四类：
  - **信号失真**：亮度、对比度、模糊、噪声、JPEG、频域扰动等，保持坐标不变。
  - **坐标对齐变化**：裁剪、旋转、翻转、透视、平移、行列删除、外绘扩展画布等，改变图像坐标框。
  - **间接局部编辑**：黑块、拼接、inpainting、outpainting 等，编辑区域独立于水印区域。
  - **直接水印编辑**：直接对水印区域进行 inpainting 或 splicing。
- **定位与同步的区分**：
  - **定位**是水印解码器自身能力，用于识别水印所在区域。
  - **同步**是独立可选模块，用于估计并逆转几何变换。本文采用 SyncSeal 作为代表性同步方法。
- **全局水印方法的局部适配**：OmniGuard、TrustMark、PixelSeal 原本面向全图水印，本文通过将嵌入残差限制在指定掩码内来适配局部水印；解码时不给真值掩码。
- **关键指标与公式**：
  - **载荷恢复**：报告 bit accuracy，并引入长度感知的 **evidence score**。若 n 位载荷中正确 k 位，随机解码至少正确 k 位的概率为  
    \(p_{\text{random}} = \Pr[\text{Binomial}(n,0.5) \ge k]\)，报告 \(-\log_{10}(p_{\text{random}})\)。分数 3 对应 \(p_{\text{random}}=10^{-3}\)，作为定性参考阈值。
  - **定位精度**：预测水印存在图阈值化到 0.5，与变换后的真值水印掩码计算 IoU。
  - **不可感知性**：在干净条件下用 PSNR、SSIM、LPIPS 衡量原始图像与水印图像的差异。

## 3. 实验设计

- **数据集**：
  - COCO 2017：日常场景，使用实例分割掩码。
  - SA-1B：大规模分割掩码，内容多样。
  - DIV2K：高质量高分辨率图像，无原生分割掩码，程序生成矩形或不规则区域。
  - MIRFLICKR-25K：真实摄影图像，无原生分割掩码，程序生成区域。
- **Benchmark 规模**：干净参考 + 55 种变换，共 56 个条件；每个方法每个数据集固定评估 200 张图像，即每方法 800 张，总嵌入图像约 4000 张。
- **对比方法**：
  - 原生局部水印：MaskWM、WAM。
  - 适配为局部水印的全局方法：OmniGuard、TrustMark、PixelSeal。
- **同步对照**：多数条件下评估 SyncSeal 开/关，以分析同步对几何变换的作用及图像质量代价。
- **评估维度**：
  - 载荷恢复：bit accuracy、evidence score。
  - 定位：MaskWM 与 WAM 的像素级 IoU。
  - 不可感知性：干净条件下 PSNR、SSIM、LPIPS。
- **额外分析**：水印区域面积影响、生成式编辑的深入分析、SyncSeal 的选择性效果。

## 4. 资源与算力

- 论文**未明确报告**使用的 GPU 型号、数量、训练时长或推理硬件。
- 方法均使用公开预训练权重，**没有进一步训练或微调**；SyncSeal 也直接使用预训练模型。
- 输入图像统一缩放至 **512×512**，水印掩码在同一坐标系生成。
- 因此，本文主要是推理与评测型工作，而非训练型工作；算力开销细节不可从文中得知。

## 5. 实验数量与充分性

- **实验规模较大**：5 种方法 × 4 个数据集 × 每数据集 200 张 ×（1 干净 + 55 变换），并包含 SyncSeal 开/关对照。粗略估计，解码评估可达数十万次量级。
- **覆盖较广**：变换涵盖信号、几何、生成式局部编辑和直接水印编辑；同时考察载荷恢复、定位和图像保真度。
- **公平性措施**：统一协议、固定随机种子、统一图像尺寸、使用预训练权重、不针对单一方法调参；变换参数不针对具体水印方案优化，因此可视为非自适应黑盒攻击。
- **仍有限制**：
  - 定位只对 MaskWM 和 WAM 直接比较，其他方法输出形态或语义不匹配。
  - 全局方法局部适配可能并非最优，存在方法适配偏差。
  - 每数据集 200 张、每个随机变换通常采样一次实现，统计稳定性和参数扫描充分性有限。
  - 生成式编辑主要基于 Stable Diffusion inpainting 管线，结果可能依赖具体生成模型和参数。

## 6. 主要结论与发现

- **没有方法对所有变换鲁棒**。所有评估方法均存在脆弱变换。
- **干净条件下**：
  - MaskWM 载荷恢复和定位最强：bit accuracy \(0.973\pm0.018\)，evidence score \(17.08\pm1.37\)，定位 IoU 0.902。
  - 但 MaskWM 干净图像质量最低：PSNR 49.48 dB，LPIPS 最高。
  - WAM 干净恢复较弱：bit accuracy 0.702，evidence score 3
